# 🗺️ الدليل التنفيذي الشامل: خطوة بخطوة لبناء نظام التنبؤ بالتعرض الحراري

هذا الملف يمثل خارطة طريق مفصلة لبناء مشروعك (Heat Exposure Prediction & Route Optimization System) من الصفر حتى مرحلة الإطلاق. لقد تم تقسيم العمل إلى **6 مراحل منطقية ومتسلسلة**. لا تنتقل لمرحلة إلا بعد إنهاء وضمان عمل المرحلة التي تسبقها.

---

## 🛠️ المرحلة الأولى: تجهيز البيئة والبنية التحتية (استغراق متوقع: يوم إلى يومين)
**الهدف:** إعداد بيئة العمل البرمجية، والمجلدات، وربط الواجهة الأمامية بالخلفية بشكل أولي.

1. **تجهيز البيئة الافتراضية:** 
   - افتح موجه الأوامر (Terminal) وأنشئ بيئة عمل افتراضية (Virtual Environment) باستخدام الأمر `python -m venv venv`.
   - قم بتفعيل البيئة (تعتمد الأوامر على نظام التشغيل الخاص بك).
2. **تثبيت المكتبات الأساسية:**
   - قم بإنشاء ملف `requirements.txt` وضع فيه الأساسيات: `fastapi, uvicorn, streamlit, osmnx, networkx, xgboost, scikit-learn, openmeteo-requests, pandas, geopandas`.
   - قم بتثبيتها عبر `pip install -r requirements.txt`.
3. **هيكلة المشروع:**
   - أنشئ المجلدات الأساسية كما في ملف الـ README الخاص بك (مثلاً مجلدات للبيانات `data_ingestion`، المميزات `feature_engineering`، نماذج الذكاء `modeling`، والتوجيه `optimization`).
4. **الاختبار الأولي (Hello World):**
   - أنشئ ملف `src/api/main.py` واكتب فيه كود بسيط بـ FastAPI يتأكد أن الخادم يعمل.
   - أنشئ ملف `app/streamlit_app.py` يكتب عنواناً بسيطاً يتأكد أن واجهة Streamlit تعمل.

---

## 🌍 المرحلة الثانية: جلب بيانات الخرائط والشبكات الجغرافية (استغراق متوقع: 3 إلى 5 أيام)
**الهدف:** تحميل الخريطة وتحويل الشوارع إلى نقاط خطية قابلة للملاحة رياضياً.

1. **تحميل الخرائط عبر OSMnx:**
   - استخدم مكتبة `osmnx` لتحميل شبكة محددة لمدينة (مثلاً شارع في الرياض أو مدينة بحالها).
   - *ماذا تفعل:* اكتب كوداً يحمل `graph_from_place("Riyadh, Saudi Arabia", network_type="drive")`.
2. **استخراج معلومات الطرق:**
   - قم بمعالجة واستخلاص خواص الطريق (نوع الشارع، السرعة القصوى، الطول).
   - حول البيانات من `Graph` إلى إطارات بيانات `GeoDataFrames` ليسهل التعامل معها في بايثون.
3. **تقطيع الطرق:**
   - كل شارع طويل يجب تقسيمه لأجزاء قصيرة (مثلاً 50 متر) لكي نستطيع حساب الحرارة والظل لكل قطعة بدقة لاحقاً.

---

## ☀️ المرحلة الثالثة: الطقس والمميزات الحرارية لهندسة البيانات (استغراق متوقع: أسبوع)
**الهدف:** أخذ الشوارع التي استخرجناها، وإضافة درجات الحرارة والظل والإشعاع الشمسي لكل شارع.

1. **جلب الطقس المباشر:**
   - اكتب دالة `fetch_current_weather(lat, lon)` تستخدم API الخاص بـ Open-Meteo لجلب الحرارة الحالية، الرطوبة، وسرعة الرياح للمدينة.
2. **المتغيرات الفلكية والشمسية (Solar Calculation):**
   - استخدم مكتبة `pvlib` أو حسابات رياضية لحساب "ارتفاع الشمس" (Solar Altitude) و"اتجاهها" (Azimuth) بناءً على الوقت الحالي والإحداثيات.
3. **معالجة التظليل (Shadow Estimator):**
   - اكتب خوارزمية مبسطة تحسب ما إذا كان مقطع الشارع واقعاً في مدى ظل مبانٍ قريبة أو أشجار بناءً على حساب زاوية الشمس. تعطيك الدالة نسبة مئوية (مثلاً: الشارع الفلاني مظلل بنسبة 40%).
4. **حساب المؤشرات الحرارية (Heat Index):**
   - قم بدمج درجة الحرارة والرطوبة وأثر التعرض الشمسي لتوليد مؤشر WBGT (درجة حرارة البصيلة الرطبة الكروية) لكل مقطع طريق.

---

## 🧠 المرحلة الرابعة: نماذج الذكاء الاصطناعي (ML Modeling) (استغراق متوقع: أسبوع - أسبوعين)
**الهدف:** التنبؤ بمعدل "الخطر الحراري" بناءً على كل البيانات التي تم جمعها.

1. **إنشاء بيانات التدريب:**
   - بما أنك لن تنتظر الصيف الفعلي، قم ببناء `Dataset` تعتمد على بيانات مناخية سابقة (ERA5) مع تصنيفات حرارية.
2. **تدريب نموذج الحرارة (Heat Exposure Model):**
   - ابنِ نموذجاً (مثل XGBoost) يأخذ المدخلات المدمجة من المراحل السابقة (نوع الشارع، الطقس المعالج، قوة الشمس والظل).
   - المخرج (Prediction) هو "درجة الخطر الحراري" لكل نقطة وتتراوح مثلاً من 1 إلى 50.
3. **التنبؤ بالزمن الأنسب للحركة (Time-series Model):**
   - اكتب سكريبت يستخدم نموذج بسيط مثل `Prophet` يأخذ درجات الحرارة المتوقعة خلال الـ 12 ساعة القادمة، ويقترح أقل ساعة خطورة للانطلاق وتأجيل الرحلة إلها.

---

## 🧭 المرحلة الخامسة: محرك التوجيه وصناعة المسار (استغراق متوقع: أسبوع)
**الهدف:** صنع خوارزمية ذكية لا تبحث عن المسار الأقصر فقط، بل تدمج الحرارة ضمن حساباتها.

1. **دالة تكلفة المسار المستحدثة:**
   - في الملاحة التقليدية: وزن الشارع (Cost) = الوقت اللازم لقطعه.
   - *ماذا تفعل:* غيّر دالة التكلفة لتصبح مدمجة: 
   `Cost = (0.3 * Travel_Time) + (0.7 * Heat_Exposure)` 
   *(يتم ضبط هذه الأوزان بناء على استراتيجية المستخدم للمسار).*
2. **تطبيق خوارزمية الملاحة (A* Search أو Dijkstra):**
   - استخدم مكتبة `NetworkX` لتشغيل خوارزمية الوصول من البداية A للنهاية B بناءً على الوزن المستحدث (Cost).
3. **إنشاء خيارات بديلة:**
   - عدل الكود ليعيد للمستخدم 3 اقتراحات دوماً: {أسرع مسار، أبرد مسار، المسار المتوازن الموصى به}.

---

## 🎨 المرحلة السادسة: واجهة المستخدم وشرح القرارات (استغراق متوقع: أسبوع)
**الهدف:** إظهار النتائج للمستخدم الـ End-User بشكل بصري، تفاعلي، ومفهوم.

1. **تصميم واجهة Streamlit:**
   - أنشئ خريط تفاعلية (باستخدام `Folium` أو `PyDeck` داخل Streamlit).
   - ارسم مساراتك بألوان مختلفة (أحمر للمسار الحار ذو التعرض العالي، وأخضر للمسار المظلل/الآمن).
2. **التوضيحات وتفسير قرار الذكاء الاصطناعي (Explainability):**
   - اكتب دالة تحلل المسار المختار وتقارنه بالأقصر. 
   - أظهر رسالة واضحة للمستخدم: *"ننصحك بالمسار ب لأنه رغم زيادة مدته بـ 3 دقائق نسبة للمسار أ المعتاد، إلا أنه يقلل تعرضك لخطر الحرارة بنسبة 40% نظراً لأن ربع الطريق مظلل وتقع حرارته في النطاق الآمن."*
3. **مراجعة الأداء (Testing):**
   - اختبر إدخال أوقات مختلفة (في ذروة الظهيرة مقابل بعد غروب الشمس) وتأكد أن سلوك الخوارزمية يختلف ويتكيف بذكاء وواقعية.

---
**نصيحة هامة:** لا تحاول بناء المشروع دفعة واحدة، ركّز على إنهاء **المرحلة الأولى ثم الثانية**، وعندما تتأكد أنه بإمكانك توليد نقاط الخريطة، انتقل للبيانات المناخية.
